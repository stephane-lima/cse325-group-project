# Compiles and Build outputs
bin/
obj/

# Local SQLite databases and engine state journals
*.db
*.db-shm
*.db-wal
budget_tracker_records.db

# Ensure the database baseline gets tracked for cloud hosting deployment
!budget_tracker_records.db
