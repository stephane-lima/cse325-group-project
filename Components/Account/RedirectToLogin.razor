@inject NavigationManager Navigation

@*
    Helper component: redirects unauthenticated users to the login page,
    preserving where they were headed via a returnUrl query parameter.
*@

@code {
    /// <summary>
    /// Redirect the current unauthenticated request to the `/login` page
    /// and include the original path as a `returnUrl` query parameter.
    /// </summary>
    protected override void OnInitialized()
    {
        var returnUrl = Navigation.ToBaseRelativePath(Navigation.Uri);
        Navigation.NavigateTo($"login?returnUrl=/{Uri.EscapeDataString(returnUrl)}", forceLoad: true);
    }
}
